// From Obfuscated C Contest

#include "helper.h"

long a = 10000, b, c = 840, d, e, f[841], g;

int main()
{
    for (; b - c;)
        f[b++] = a / 5;
    for (; d = 0, g = c * 2; c -= 14, output_num_4((e + d / a) % a), e = d % a)
        for (b = c; d += f[b] * a, f[b] = d % --g, d /= g--, --b; d *= b)
            ;
    return 0;
}
